// Applied to every screen heading. On a near-black background a dark drop
// shadow is invisible — pop comes from pure white + a soft glow + full bold.
export const headingShadow = {
  color: '#FFFFFF',
  fontFamily: 'Inter_700Bold',
  textShadowColor: 'rgba(240, 242, 244, 0.30)',
  textShadowOffset: { width: 0, height: 0 },
  textShadowRadius: 14,
};
